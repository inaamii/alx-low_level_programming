#include <stdio.h>
/**
 * main - main prints the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char n = 'a';
		while (n <= 'z')
	{
		putchar(n);
		n++;
	putchar('\n');
	}
	return (0);
}
