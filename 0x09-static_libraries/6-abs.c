#include "main.h"
/**
 * _abs - a function that computes
 * the absolute value of an integer.
 * @n: n number
 *
 * Return: absolute value
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n == 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
