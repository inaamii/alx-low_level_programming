#include "main.h"

/**
 * add - a function that adds tow integers and return the result
 *
 * @a: first integer
 * @b: one more integer
 *
 * Return: result of sum
 */

int add(int a, int b)
{
	int sum = a + b;
		return (sum);
}
