#include "main.h"

/**
 * mul - a function that multiplies two integers
 *
 * @a : for check the input
 * @b : for check the input
 *
 * Return: return a multiplies
 */
int mul(int a, int b)
{
	return (a * b);
}
